let a : number;
let b : boolean;
let c : string;

type User ={
    name : string;
    surname: string;
    age: number;
    isAdmin : boolean;
};

const user : User ={
    name:'nick',
    surname:'anass',
    age : 16,
    isAdmin:true,
};